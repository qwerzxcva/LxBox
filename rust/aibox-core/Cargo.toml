[package]
name = "aibox-core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "aibox_core"

[dependencies]
jni = "0.21"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rsxm-rules = { path = "../rsxm/crates/rsxm-rules" }
